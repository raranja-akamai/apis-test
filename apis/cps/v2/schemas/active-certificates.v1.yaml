type: object
description: |-
  Lists enrollments with details on active certificates.
  
x-akamai:
  file-path: schemas/active-certificates.v1.yaml
additionalProperties: false
required:
- enrollments
properties:
  enrollments:
    type: array
    description: |
      Enrollments with active certificates.
    items:
      $ref: active-certificate.v1.yaml
