name: network
in: query
description: |-
  The selected network, either `staging` or `prod`.
  
x-akamai:
  file-path: parameters/network-query.yaml
schema:
  type: string
  example: staging
  enum:
  - staging
  - prod
