type: object
additionalProperties: false
required:
- matchRules
properties:
  description:
    type: string
    description: |
      A human-readable label for the policy version.
  matchRules:
    description: |
      A list of Cloudlet-specific match rules for this shared policy. Currently, you can only create match rules only for the Edge Redirector, Forward Rewrite, and Audience Segmentation Cloudlets.
    $ref: match-rules.yaml
