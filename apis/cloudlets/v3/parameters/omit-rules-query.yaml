name: omitRules
in: query
description: |-
  Excludes the match rules from the results. Defaults to `false`.
  
x-akamai:
  file-path: parameters/omit-rules-query.yaml
schema:
  type: boolean
  example: true
  default: false
