description: 'Including match rules for all policy versions in the results is now
  disabled. You can get match rules for a single policy version using [Get a Policy
  Version](https://techdocs.akamai.com/cloudlets/v2/reference/get-policy-version).

  '
in: query
name: includeRules
schema:
  default: false
  example: true
  type: boolean
x-akamai:
  file-path: parameters/include-rules-query.yaml
