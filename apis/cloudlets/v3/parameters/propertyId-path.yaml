name: propertyId
in: path
description: |
  The ID of the property linked with the `policyId`. For the `propertyId` value, use the `id` value from the [Get associated properties for a policy](ref:get-policy-properties) response. 
x-akamai:
  file-path: parameters/propertyId-path.yaml
required: true
schema:
  type: integer
  example: 987654321
