description: 'If cloning an existing policy version, this parameter value is the number
  of the policy version you want to clone. If there are attributes that you do _not_
  want propagated from the source policy version, you must provide the new values
  in the request.

  '
in: query
name: cloneVersion
schema:
  example: 22
  type: integer
x-akamai:
  file-path: parameters/clone-version-query.yaml
