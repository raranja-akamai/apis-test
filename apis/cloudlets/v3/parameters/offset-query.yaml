description: 'When requesting pages of data, `offset` is the record after which to
  start. Use `offset` with `pageSize`. For example: offset=25&pageSize=15 starts at
  record 26 and displays up to 15 records for each property the policy applies to.

  '
in: query
name: offset
required: false
schema:
  default: 0
  example: 25
  minimum: 0
  type: integer
x-akamai:
  file-path: parameters/offset-query.yaml
