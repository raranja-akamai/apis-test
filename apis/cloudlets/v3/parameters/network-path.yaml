name: network
in: path
description: |
  Returns only activations for the selected network, either `staging` or `production`.
required: true
schema:
  type: string
  example: staging
  enum:
  - staging
  - production
