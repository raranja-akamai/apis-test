description: 'Returns data for a specific type of origin as defined in Property Manager.
  Options include `alb` for Application Load Balancer origins, `customer` for standard
  origins, and `netstorage` for NetStorage-based origins.

  '
in: query
name: type
schema:
  example: alb
  type: string
x-akamai:
  file-path: parameters/type-query.yaml
