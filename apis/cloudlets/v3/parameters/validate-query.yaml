description: 'For GET operations, verifies that the current settings for the selected
  `originId` and `version` are valid.

  '
in: query
name: validate
schema:
  example: true
  type: boolean
x-akamai:
  file-path: parameters/validate-query.yaml
