description: 'For POST operations, indicates the version of the existing policy to
  use for the new policy.  If not specified, the latest version of the existing policy
  is copied.

  '
in: query
name: version
schema:
  example: 2
  type: integer
x-akamai:
  file-path: parameters/version-query.yaml
