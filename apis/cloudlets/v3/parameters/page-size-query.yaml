name: pageSize
in: query
description: |
  The number of records to return. Use with `offset` to manage the number of records returned.
required: false
schema:
  type: integer
  example: 15
  default: 100
  minimum: 5
  maximum: 1000
