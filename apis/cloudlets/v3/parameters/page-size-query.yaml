description: 'The number of records to return. Use with `offset` to manage the number
  of records returned.

  '
in: query
name: pageSize
required: false
schema:
  default: 100
  example: 15
  maximum: 1000
  minimum: 5
  type: integer
x-akamai:
  file-path: parameters/page-size-query.yaml
