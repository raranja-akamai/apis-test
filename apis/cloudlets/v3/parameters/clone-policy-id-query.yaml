description: 'If cloning an existing policy, this parameter is the ID of the policy
  (`policyId`) you want to clone. If there are attributes that you do _not_ want propagated
  from the source policy, you must provide the new values in the request.

  '
in: query
name: clonePolicyId
schema:
  example: 1001
  type: integer
x-akamai:
  file-path: parameters/clone-policy-id-query.yaml
