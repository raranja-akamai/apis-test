description: 'The order within the current list of rules where you want to add the
  new rule. If you do not set this parameter, the new rule is added to the end of
  the current rule list. For Cloudlets, the first rule listed is the first rule evaluated.

  '
in: query
name: index
schema:
  example: 3
  type: integer
x-akamai:
  file-path: parameters/index-query.yaml
