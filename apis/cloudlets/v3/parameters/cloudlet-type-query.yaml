description: 'The two- or three- letter code of the Cloudlet you want to view all
  schemas for. This value corresponds to the `cloudletCode` member available from
  [List Cloudlets](ref:get-cloudlets).

  '
in: query
name: cloudletType
schema:
  example: ER
  type: string
x-akamai:
  file-path: parameters/cloudlet-type-query.yaml
