description: 'The selected network, either `staging` or `prod`.

  '
in: query
name: network
schema:
  enum:
  - staging
  - prod
  example: staging
  type: string
x-akamai:
  file-path: parameters/network-query.yaml
