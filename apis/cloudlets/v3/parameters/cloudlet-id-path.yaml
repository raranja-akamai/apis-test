description: 'For GET operations, returns only policies for this particular `cloudletId`.
  The `cloudletId` for Edge Redirector is `0`, Visitor Prioritization is `1`, Forward
  Rewrite is `3`, Request Control is `4`, API Prioritization is `5`, Audience Segmentation
  is `6`, Phased Release is `7` and Application Load Balancer is `9`.

  '
in: path
name: cloudletId
required: true
schema:
  enum:
  - 0
  - 1
  - 3
  - 4
  - 5
  - 6
  - 7
  - 9
  example: 0
  type: integer
x-akamai:
  file-path: parameters/cloudlet-id-path.yaml
