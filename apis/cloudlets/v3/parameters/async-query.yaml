description: 'Set `async` to `true` for policy activations on multiple properties
  to reduce API response time and let the policy activation continue asynchronously
  in the background. Default is `false`.

  '
in: query
name: async
schema:
  default: false
  example: true
  type: boolean
x-akamai:
  file-path: parameters/async-query.yaml
