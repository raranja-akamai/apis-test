description: List of current load balancing activations on `STAGING`, and `PRODUCTION`
  networks.
items:
  $ref: origin-activation.yaml
type: array
x-akamai:
  file-path: schemas/get-origin-activations-response.yaml
