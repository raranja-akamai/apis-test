definitions:
  link:
    additionalProperties: false
    properties:
      href:
        description: 'The link being expressed.

          '
        format: uri
        type: string
      rel:
        description: 'The relation this link has to the resource.

          '
        type: string
    required:
    - href
    - rel
x-akamai:
  file-path: schemas/links-definitions.yaml
