additionalProperties: false
description: "Contains information about the newest property version and the property\u2019\
  s current configuration on the production and staging networks.\n"
properties:
  groupId:
    description: 'The group association for the property.

      '
    type: integer
  id:
    description: 'Unique identifier for the property.

      '
    type: integer
  name:
    description: 'The name of the property. This value also appears at the start of
      each separate property object.

      '
    type: string
  newestVersion:
    description: 'Contains information about the newest property version.

      '
    items:
      $ref: property.yaml
    type: array
  production:
    description: "Contains information about the property\u2019s current configuration\
      \ on the `production` network.\n"
    items:
      $ref: property.yaml
    type: array
  staging:
    description: "Contains information about the property\u2019s current configuration\
      \ on the `staging` network.\n"
    items:
      $ref: property.yaml
    type: array
required:
- groupId
- name
- newestVersion
type: object
x-akamai:
  file-path: schemas/get-properties-response.yaml
