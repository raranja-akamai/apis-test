additionalProperties: false
description: 'Provides information on the current load balancing activations.

  '
properties:
  PRODUCTION:
    description: 'Contains information for current load balancing activations on the
      `PRODUCTION` network.

      '
    items:
      $ref: origin-currentactivations.yaml
    type: array
  STAGING:
    description: 'Contains information for current load balancing activations on the
      `STAGING` network.

      '
    items:
      $ref: origin-currentactivations.yaml
    type: array
  originId:
    description: 'The conditional origin''s unique identifier for the current load
      balancing activation on either or both the `PRODUCTION` and `STAGING` networks.

      '
    type: string
type: object
x-akamai:
  file-path: schemas/get-origins-currentactivations-response.yaml
