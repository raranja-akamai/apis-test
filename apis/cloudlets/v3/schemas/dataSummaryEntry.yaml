additionalProperties: false
description: 'TBD.

  '
properties:
  maximum:
    description: 'Maximum requests across all time intervals.

      '
    minimum: 0
    type: integer
  mean:
    description: 'Average requests across all time intervals.

      '
    minimum: 0
    type: number
  minimum:
    description: 'Minimum requests across all time intervals.

      '
    minimum: 0
    type: integer
  originId:
    description: 'The identifier of an origin.

      '
    type: string
  percentAlive:
    description: 'Percent of time intervals where this data center was considered
      "alive".

      '
    maximum: 100
    minimum: 0
    type: number
  percentShare:
    description: 'Percent of all requests served by this data center across all time
      intervals.

      '
    minimum: 0
    type: number
  requests:
    description: 'Number of requests served by this data center across all time intervals.

      '
    minimum: 0
    type: integer
  requestsPerSecond:
    description: 'Average requests per second served by this data center across all
      time intervals.

      '
    minimum: 0
    type: number
required:
- maximum
- minimum
- mean
- percentShare
- requests
- requestsPerSecond
type: object
x-akamai:
  file-path: schemas/dataSummaryEntry.yaml
