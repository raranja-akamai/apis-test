additionalProperties: false
description: 'Application Load Balancer configuration.

  '
properties:
  checksum:
    description: 'The checksum that distinguishes this conditional origin from any
      others that might share the same name.

      '
    type: string
  description:
    description: 'The description of the load balancing configuration provided by
      a user.

      '
    type: string
  originId:
    description: 'The conditional origin''s unique identifier.

      '
    maxLength: 63
    minLength: 2
    type: string
  type:
    description: 'The type of origin this conditional origin is set up as. In this
      response the conditonal origin `type` is `APPLICATION_LOAD_BALANCER`.

      '
    enum:
    - APPLICATION_LOAD_BALANCER
    type: string
type: object
x-akamai:
  file-path: schemas/get-origin-response.yaml
