type: object
description: |
  TBD.
x-akamai:
  file-path: schemas/post-policy-versions-request.yaml
additionalProperties: false
required:
- matchRules
properties:
  description:
    type: string
    description: |
      Description of the policy version.
  matchRuleFormat:
    type: string
    description: |
      The version of the `matchRules` format.
  matchRules:
    $ref: match-rules.yaml
