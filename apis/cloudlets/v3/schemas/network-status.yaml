additionalProperties: false
description: The policy version status on the production and staging networks.
properties:
  effective:
    $ref: policy-activation.yaml
    description: 'The status of the activation that''s currently in use on this network,
      or `null` if the policy has no activations.

      '
  latest:
    $ref: policy-activation.yaml
    description: The status of the most recent activation or deactivation operation
      for the policy on this network. If the policy has no activations, `null` is
      returned.
required:
- effective
- latest
type: object
x-akamai:
  file-path: schemas/network-status.yaml
