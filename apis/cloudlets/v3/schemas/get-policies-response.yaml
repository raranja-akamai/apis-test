type: object
additionalProperties: false
required:
- content
- page
properties:
  content:
    type: array
    description: |
      Lists the results of the request.
    items:
      $ref: policy.yaml
  links:
    $ref: links.yaml
  page:
    $ref: page-head.yaml
