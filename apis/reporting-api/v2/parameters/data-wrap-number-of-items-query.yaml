name: dataWrapNumberOfItems
in: query
description: |
  For reports that allow non-top values to cluster, this specifies the number of top values to report on separately.
schema:
  type: integer
  example: 5
