name: end
in: query
description: |-
  End date in ISO 8601 format. Use this parameter together with `start`, or else replace both with a fixed `timeRange`.
  
x-akamai:
  file-path: parameters/end-query.yaml
schema:
  type: string
  example: '2024-06-02T00:00:00Z'
  format: date-time
