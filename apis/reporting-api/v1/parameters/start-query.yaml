name: start
in: query
description: |
  Start date in ISO 8601 format. Use this parameter together with `end`, or else replace both with a fixed `timeRange`.
schema:
  type: string
  example: '2024-06-02T00:00:00Z'
  format: date-time
