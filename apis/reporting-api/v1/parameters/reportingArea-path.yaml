name: reportingArea
in: path
description: |-
  The grouping of reports related to the same area.
  
x-akamai:
  file-path: parameters/reportingArea-path.yaml
required: true
schema:
  type: string
  example: traffic
