name: async
in: query
description: |-
  Enables the asynchronous flow to get the data. By default, `false`.
  
x-akamai:
  file-path: parameters/async-query.yaml
schema:
  type: boolean
  example: true
  default: false
