type: string
description: |-
  Type of values, either `STRING`, `LONG`, `DOUBLE`, `TIMESTAMP_SEC`, `TIMESTAMP_MS`, or `DATE_ISO8601`.
  
x-akamai:
  file-path: schemas/type-enum.yaml
enum:
- STRING
- LONG
- DOUBLE
- TIMESTAMP_SEC
- TIMESTAMP_MS
- DATE_ISO8601
