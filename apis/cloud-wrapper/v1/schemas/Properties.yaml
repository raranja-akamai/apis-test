type: object
description: |
  Eligible properties that don't have Cloud Wrapper enabled.
additionalProperties: false
required:
- properties
properties:
  properties:
    type: array
    description: |
      All eligible properties that don't have Cloud Wrapper enabled.
    minItems: 0
    items:
      $ref: ../schemas/Property.yaml
