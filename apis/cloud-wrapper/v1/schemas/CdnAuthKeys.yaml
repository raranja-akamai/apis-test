type: object
description: |
  Auth keys for a specified CDN.
additionalProperties: false
required:
- cdnAuthKeys
properties:
  cdnAuthKeys:
    type: array
    description: |
      Auth keys for the specified CDN.
    minItems: 0
    items:
      $ref: ../schemas/CdnAuthKey.yaml
