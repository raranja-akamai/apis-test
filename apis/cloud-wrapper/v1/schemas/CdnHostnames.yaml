type: object
description: |
  Origin hostnames for a configuration.
additionalProperties: false
required:
- hostnames
properties:
  hostnames:
    type: array
    description: |
      All available CDN providers.
    minItems: 0
    items:
      $ref: ../schemas/CdnHostname.yaml
