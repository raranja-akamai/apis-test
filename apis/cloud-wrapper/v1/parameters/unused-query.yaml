name: unused
in: query
description: |
  Specify whether the response should contain only unused properties, `false` by default.
required: false
schema:
  type: boolean
  example: true
  default: false
