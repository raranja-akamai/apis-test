type: object
description: |-
  Completes traditional registration. After registering, the user can authenticate by supplying an email address and password.
  
x-akamai:
  file-path: schemas/post-oauth-register-native-traditional-response.yaml
additionalProperties: false
required:
- created
- uuid
- state
- access_token
properties:
  access_token:
    type: string
    description: |
      Valid access token for the user.
    minLength: 1
  created:
    type: string
    description: |
      Timestamp indicating when the user account was created.
    minLength: 1
    format: date-time
  stat:
    type: string
    description: |
      Status of the operation. Possible values are: `ok`, `error`, and `fail`. The value `ok` indicates that the operation was successful. The value `error` indicates that the operation couldn't complete because of a problem with the request. The value `fail` means that the operation couldn't complete because of an error with the Identity Cloud backend infrastructure. Contact your Identity Cloud representative if your request returns the `fail` status.
    enum:
    - ok
    - error
    - fail
    minLength: 1
  uuid:
    type: string
    description: |
      Universally unique identifier of the user profile.
    minLength: 1
