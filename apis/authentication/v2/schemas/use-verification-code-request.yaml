type: object
description: |-
  Uses a verification code to set a date-time attribute to the current time. You typically use this operation to verify email addresses.
  
x-akamai:
  file-path: schemas/use-verification-code-request.yaml
additionalProperties: false
required:
- verification_code
properties:
  verification_code:
    type: string
    description: |
      Verification code from the [Get verification code](ref:post-access-getverificationcode) operation.
    minLength: 1
