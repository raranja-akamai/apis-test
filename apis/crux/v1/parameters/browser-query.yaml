name: browser
in: query
description: |
  Filter records by browser. Either `edge`, `firefox`, `chrome`, or `safari`.
schema:
  type: string
  example: chrome
  enum:
  - edge
  - firefox
  - chrome
  - safari
