type: object
description: |
  Directory user search results.
additionalProperties: false
required:
- user
properties:
  user:
    type: string
    description: |
      The directory user.
    minLength: 1
