type: object
description: |
  List of directory users.
additionalProperties: false
required:
- meta
- objects
properties:
  meta:
    $ref: pageable.yaml
  objects:
    type: array
    description: |
      Directory users.
    minItems: 1
    items:
      $ref: user.yaml
