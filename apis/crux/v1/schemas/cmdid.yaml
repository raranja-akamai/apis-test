type: object
description: |
  Command ID for the task.
additionalProperties: false
required:
- cmdid
properties:
  cmdid:
    type: string
    description: |
      A unique identifier to track command progress.
    minLength: 1
