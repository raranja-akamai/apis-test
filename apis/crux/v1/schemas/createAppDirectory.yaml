type: object
description: |
  Creates an application directory.
additionalProperties: false
required:
- data
properties:
  data:
    type: array
    description: |
      Application directory details.
    minItems: 1
    items:
      type: string
      minLength: 1
