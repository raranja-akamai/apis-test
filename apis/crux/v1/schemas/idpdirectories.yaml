type: object
description: |
  Enterprise Access IDP directory data.
additionalProperties: false
required:
- meta
- objects
properties:
  meta:
    $ref: pageable.yaml
  objects:
    type: array
    description: |
      IDP directory data.
    minItems: 1
    items:
      $ref: idpdirectory.yaml
