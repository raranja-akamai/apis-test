type: object
description: |
  List of certificates.
additionalProperties: false
required:
- meta
- objects
properties:
  meta:
    $ref: pageable.yaml
  objects:
    type: array
    description: |
      Certificate details.
    minItems: 0
    items:
      $ref: certificate_post.yaml
