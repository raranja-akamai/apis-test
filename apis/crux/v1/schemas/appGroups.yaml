type: object
description: |
  List of Enterprise Access Application Groups.
required:
- meta
- objects
properties:
  meta:
    $ref: pageable.yaml
  objects:
    type: array
    items:
      $ref: app-group.yaml
