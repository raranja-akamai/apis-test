type: object
description: |
  Users to re-invite in bulk.
additionalProperties: false
required:
- user_list
properties:
  user_list:
    type: array
    description: |
      Users to re-invite.
    minItems: 1
    items:
      type: string
      minLength: 1
