type: object
description: |-
  Rule statistics by rule ID.
  
x-akamai:
  file-path: schemas/rule-stats-by-rule-id-response.yaml
additionalProperties: false
properties:
  bytes:
    type: integer
    description: |
      The rule size in bytes.
  firstMatch:
    type: string
    description: |
      The first rule match.
  lastMatch:
    type: string
    description: |
      The last rule match.
  lastUpdated:
    type: string
    description: |
      The user who last updated the rule.
  matches:
    type: integer
    description: |
      The rule matches.
  ruleId:
    type: string
    description: |
      The rule ID.
