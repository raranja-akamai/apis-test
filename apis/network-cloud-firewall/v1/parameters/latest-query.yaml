name: latest
in: query
description: |-
  Returns the latest ACL version for the account. If `true`, displays only ACL versions in `CONFIGURATION` and `DEPLOYED` states.
  
x-akamai:
  file-path: parameters/latest-query.yaml
required: false
schema:
  type: boolean
  example: true
  default: false
