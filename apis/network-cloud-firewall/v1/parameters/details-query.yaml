name: details
in: query
description: |-
  When set to `true`, returns parsed rule details for every rule in the rule set for a version.
  
x-akamai:
  file-path: parameters/details-query.yaml
schema:
  type: boolean
  example: true
  default: false
