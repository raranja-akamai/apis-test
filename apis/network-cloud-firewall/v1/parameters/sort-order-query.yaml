name: sortOrder
in: query
description: |-
  The results sort order. Either `asc` (ascending) or `desc` (descending).
  
x-akamai:
  file-path: parameters/sort-order-query.yaml
required: false
schema:
  type: string
  example: asc
  enum:
  - asc
  - desc
