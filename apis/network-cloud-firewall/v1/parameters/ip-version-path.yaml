name: ipVersion
in: path
description: |-
  The IP version, either `V4` or `V6`.
  
x-akamai:
  file-path: parameters/ip-version-path.yaml
required: true
schema:
  type: string
  example: V4
  enum:
  - V4
  - V6
