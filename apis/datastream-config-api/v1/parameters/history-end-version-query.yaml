name: endVersion
in: query
description: |-
  Specifies the last stream version in the range you want to fetch stream history details for.
  
x-akamai:
  file-path: parameters/history-end-version-query.yaml
required: false
schema:
  type: integer
  example: 10
  default: 1
