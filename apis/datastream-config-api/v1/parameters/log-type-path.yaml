name: logType
in: path
description: |-
  Defines the stream's log type: CDN, EdgeWorkers, Edge DNS or Global Traffic Management logs. For details, see [Log types](ref:log-types).
  
x-akamai:
  file-path: parameters/log-type-path.yaml
required: true
schema:
  type: string
  example: cdn
  enum:
  - cdn
  - edgeworkers
  - edns
  - gtm
