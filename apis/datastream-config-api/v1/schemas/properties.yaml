type: object
description: |-
  Lists the delivery properties on your account.
  
x-akamai:
  file-path: schemas/properties.yaml
title: CDN properties
additionalProperties: false
required:
- groupId
- objects
properties:
  groupId:
    type: integer
    description: |
      Identifies the group that the properties belong to.
  objects:
    type: array
    description: |
      Lists properties (also referred to as _objects_) within access groups.
    items:
      $ref: property.yaml
