additionalProperties: false
description: 'Provides information about a type of stream that you can configure to
  monitor your properties. `RAW_LOGS` is the only stream type available at this time.

  '
properties:
  isRaw:
    description: 'Whether the stream collects raw data.

      '
    type: boolean
  streamType:
    description: 'Specifies the stream type name. `RAW_LOGS` is the only stream type
      name currently available.

      '
    enum:
    - RAW_LOGS
    type: string
  streamTypeName:
    description: 'The name of the stream type. `Logs - Raw` is the only stream type
      name currently available.

      '
    type: string
type: object
x-akamai:
  file-path: schemas/stream-type.yaml
