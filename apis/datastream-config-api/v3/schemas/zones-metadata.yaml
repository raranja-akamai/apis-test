additionalProperties: false
description: 'The response''s additional metadata.

  '
properties:
  lastPage:
    default: 1
    description: 'The number of pages with Edge DNS zones.

      '
    maximum: 100
    minimum: 1
    type: integer
  page:
    description: 'The number of pages with zones.

      '
    minimum: 1
    type: integer
  pageSize:
    default: 100
    description: 'The number of records on each zones page.

      '
    maximum: 200
    minimum: 1
    type: integer
  totalElements:
    description: 'The total number of zones in the response.

      '
    example: 92
    minimum: 1
    type: integer
type: object
x-akamai:
  file-path: schemas/zones-metadata.yaml
