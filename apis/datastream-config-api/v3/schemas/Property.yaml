type: object
description: |
  Provides details about a CDN property.
additionalProperties: false
required:
- objectId
- objectName
- productId
- productName
- hostnames
- contractId
properties:
  objectId:
    type: integer
    description: |
      The property's unique identifier.
  objectName:
    type: string
    description: |
      The descriptive label for the property.
  productId:
    type: string
    description: |
      Identifies the product that the property belongs to.
  productName:
    type: string
    description: |
      The product's name.
  hostnames:
    type: array
    description: |
      Identifies the hostnames of active properties on the production network. If there are no active properties, this lists hostnames of active properties in staging.
    items:
      type: string
  contractId:
    type: string
    description: |
      Identifies the contract that the property belongs to.
