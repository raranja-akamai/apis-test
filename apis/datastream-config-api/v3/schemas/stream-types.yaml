description: 'Provides information about types of stream that you can configure to
  monitor your properties. `RAW_LOGS` is the only stream type available at this time.

  '
items:
  $ref: stream-type.yaml
type: array
x-akamai:
  file-path: schemas/stream-types.yaml
