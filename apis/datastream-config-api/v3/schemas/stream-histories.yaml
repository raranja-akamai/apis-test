description: 'Details of the latest stream configuration version. Data includes properties,
  data set fields, destination setup, activation status, and log format.

  '
items:
  $ref: stream-detail.yaml
type: array
x-akamai:
  file-path: schemas/stream-histories.yaml
