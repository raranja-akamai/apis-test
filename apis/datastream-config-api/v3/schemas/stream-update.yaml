additionalProperties: false
description: 'Provides information about the stream version key that consists of the
  stream''s identifier.

  '
properties:
  streamVersionKey:
    additionalProperties: false
    description: 'Specifies the primary key consisting of the stream and version identifiers.

      '
    properties:
      streamId:
        description: 'Identifies the stream.

          '
        type: integer
      streamVersionId:
        description: 'Identifies the version of the stream.

          '
        type: integer
    type: object
type: object
x-akamai:
  file-path: schemas/stream-update.yaml
