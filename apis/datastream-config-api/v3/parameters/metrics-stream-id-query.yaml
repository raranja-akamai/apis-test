name: streamId
in: query
description: |-
  Filters down metrics for a specific stream, or `groupId` for all streams belonging to that group. If you omit both, the operation returns metrics for all streams that belong to your contract.
  
x-akamai:
  file-path: parameters/metrics-stream-id-query.yaml
required: false
schema:
  type: integer
  example: 7050
