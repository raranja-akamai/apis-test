name: groupId
in: query
description: |-
  Set the `groupId` to get metrics for all streams belonging to that group, or `streamId` for a specific stream. If you omit both, this operation returns metrics for all streams that belong to your contract.
  
x-akamai:
  file-path: parameters/metrics-group-id-query.yaml
required: false
schema:
  type: integer
  example: 54321
