type: object
description: |-
  A Linode bulk log export destination.
  
x-akamai:
  file-path: schemas/get-bulk-log-export-destination-linode-200.yaml
title: Linode destination get response
additionalProperties: false
properties:
  url:
    type: string
    description: |
      The bucket URL.
  accessKeyId:
    type: string
    description: |
      The access key ID.
  accessKeySecret:
    type: string
    description: |
      The access key secret.
  destinationPath:
    type: string
    description: |
      The storage bucket name.
  encryptionToken:
    type: string
    description: |
      The encryption token.
