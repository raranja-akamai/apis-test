type: object
description: |-
  Returns all the providers for a social login application.
  
x-akamai:
  file-path: schemas/get-all-providers.yaml
additionalProperties: false
properties:
  callback:
    type: boolean
    description: |
      If `true`, the operation returns data by using the JSON with Padding (JSONP) format. This format helps you avoid cross-domain issues when sending and receiving JSON values. Only use JSONP on a secure connection.
    default: false
