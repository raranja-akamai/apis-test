type: object
description: |-
  Removes a social login identity provider from a user account.
  
x-akamai:
  file-path: schemas/unmap.yaml
additionalProperties: false
required:
- stat
properties:
  stat:
    type: string
    description: |
      Status of the operation. An `ok` value indicates the operation succeeded. An `error` means the operation couldn't complete because of a problem with the request. A `fail` value means it couldn't complete due to an error with the Identity Cloud back end. In that case, contact your Identity Cloud representative.
    enum:
    - ok
    - error
    - fail
    minLength: 1
