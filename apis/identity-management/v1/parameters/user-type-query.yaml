name: userType
in: query
description: |
  Filters the list by users who have `lostAccess` or the reverse `gainAccess`.
schema:
  type: string
  example: lostAccess
  enum:
  - lostAccess
  - gainAccess
