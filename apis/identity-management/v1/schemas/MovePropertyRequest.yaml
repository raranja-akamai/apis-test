additionalProperties: false
description: Describes the request body for moving a property under another group.
properties:
  destinationGroupId:
    description: 'Identifies the group within account hierarchy where you want to
      move the `sourceGroupId`. It can''t be a child or descendant of the current
      group.

      '
    type: integer
  sourceGroupId:
    description: 'Identifies the nested group you want to move to the `destinationGroupId`.

      '
    type: integer
required:
- sourceGroupId
- destinationGroupId
type: object
x-akamai:
  file-path: schemas/MovePropertyRequest.yaml
