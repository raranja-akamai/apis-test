additionalProperties: false
description: This object encapsulates all the users who needs to be blocked based
  on UI identities.
properties:
  uiIdentityId:
    description: '__Read-only__ A unique identifier for a user. Also known as a `contactId`
      in other APIs.

      '
    readOnly: true
    type: string
required:
- uiIdentityId
type: object
x-akamai:
  file-path: schemas/user-block-ui-identities.yaml
