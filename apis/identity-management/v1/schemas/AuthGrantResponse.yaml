additionalProperties: false
description: A user's role assignments, per group. If you provide this member in a
  POST request, don't include the `cloneFrom` member in the request too.
properties:
  groupId:
    description: 'A unique identifier for each group.

      '
    type: integer
  groupName:
    description: 'A group''s name.

      '
    type: string
  isBlocked:
    description: 'Indicates whether a user''s access is blocked for a group.

      '
    type: boolean
  roleDescription:
    description: 'Descriptive text for the role.

      '
    type: string
  roleId:
    description: 'A unique identifier for each role.

      '
    type: integer
  roleName:
    description: 'The role''s name.

      '
    type: string
  subGroups:
    description: 'Children of the parent group, represented as an array of groups.
      Permissions cascade downward from parent to child unless the child group''s
      `isBlocked` is `true`.

      '
    items:
      $ref: groups-nested.yaml
    type: array
type: object
x-akamai:
  file-path: schemas/AuthGrantResponse.yaml
