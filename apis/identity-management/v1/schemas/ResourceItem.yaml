additionalProperties: false
description: Encapsulates information about resources.
properties:
  modifiedDate:
    description: 'ISO 8601 timestamp indicating when the resource was last updated.

      '
    format: date-time
    type: string
  resourceId:
    description: 'Unique identifier for each resource.

      '
    type: integer
  resourceName:
    description: 'The name you supply for the resource.

      '
    type: string
  resourceType:
    description: 'The type of the resource, either `cname`, `arlfile`, `cpcode`, `storagegroup`,
      `fpdomain`, or `edns`.

      '
    enum:
    - cname
    - arlfile
    - cpcode
    - storagegroup
    - fpdomain
    - edns
    type: string
type: object
x-akamai:
  file-path: schemas/ResourceItem.yaml
