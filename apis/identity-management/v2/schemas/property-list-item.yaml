additionalProperties: false
description: 'Encapsulates information about a property.

  '
properties:
  actions:
    additionalProperties: false
    description: 'Encapsulates permissions available to the user for this group. This
      data is available when you specify the `actions` parameter of the same name,
      not to be confused with the `action` parameter, which applies to the [Set two-factor
      authentication](ref:put-user-profile-tfa) operation.

      '
    properties:
      move:
        description: 'Move a property from one group to another group.

          '
        type: boolean
    type: object
  groupId:
    description: 'A unique identifier for each group.

      '
    type: integer
  groupName:
    description: 'A group''s name.

      '
    type: string
  propertyId:
    description: 'A unique identifier for each property.

      '
    type: integer
  propertyName:
    description: 'The name for a property.

      '
    type: string
  propertyTypeDescription:
    description: 'Identifies the product-type such as `On Demand Media`, `Site`, `Application`,
      `Live Media Streams`, or `NetStorage`.

      '
    type: string
required:
- propertyId
- groupId
- groupName
- propertyName
- propertyTypeDescription
- actions
type: object
x-akamai:
  file-path: schemas/property-list-item.yaml
