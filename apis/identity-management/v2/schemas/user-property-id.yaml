additionalProperties: false
description: 'This object encapsulates all the users of a property by property ID.

  '
properties:
  firstName:
    description: 'The user''s first name.

      '
    type: string
  isBlocked:
    description: 'Shows if the user is blocked.

      '
    type: boolean
  lastName:
    description: 'The user''s last name.

      '
    type: string
  uiIdentityId:
    description: 'A unique identifier for a user. Also known as a `contactId` in other
      APIs.

      '
    type: string
  uiUserName:
    description: 'The full name for the user, as it appears in interfaces.

      '
    type: string
required:
- uiUserName
- uiIdentityId
- firstName
- lastName
- isBlocked
type: object
x-akamai:
  file-path: schemas/user-property-id.yaml
