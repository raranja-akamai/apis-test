additionalProperties: false
description: 'Specifies an available role.

  '
properties:
  actions:
    additionalProperties:
      description: 'The actions you can take on this role. A value of `true`  for
        `edit` means the role can be edited. You can edit a role even if it''s already
        assigned to users. Enabling `delete` means the role can be deleted. You can
        only delete a role that''s not assigned to any users. Disabling `delete` indicates
        the role can''t be deleted because it''s assigned to at least one user.

        '
      type: boolean
    description: 'Encapsulates permissions available to the user for this group.

      '
    type: object
  createdBy:
    description: 'The username or email of the person who created the role.

      '
    type: string
  createdDate:
    description: 'ISO 8601 timestamp indicating when the role was originally created.

      '
    format: date-time
    type: string
  modifiedBy:
    description: 'The username or email of the last person to edit the role.

      '
    type: string
  modifiedDate:
    description: 'ISO 8601 timestamp indicating when the role was last updated.

      '
    format: date-time
    type: string
  numUsers:
    description: 'The number of users who use the role.

      '
    type: integer
  roleDescription:
    description: 'The description for a role. The description should be meaningful
      and convey what you use the role for.

      '
    type: string
  roleId:
    description: 'A unique identifier for each role.

      '
    type: integer
  roleName:
    description: 'The role''s name.

      '
    type: string
  type:
    description: 'Indicates whether it''s a `standard` role provided by Akamai or
      a `custom` role for you to manage.

      '
    enum:
    - standard
    - custom
    type: string
  users:
    description: 'User details.

      '
    items:
      additionalProperties: false
      properties:
        accountId:
          description: 'A unique identifier for each account.

            '
          type: string
        email:
          description: 'The user''s email address.

            '
          format: email
          type: string
        firstName:
          description: 'The user''s first name.

            '
          type: string
        lastLoginDate:
          description: 'ISO 8601 timestamp indicating when the user last logged in.

            '
          format: date-time
          type: string
        lastName:
          description: 'The user''s last name.

            '
          type: string
        uiIdentityId:
          description: 'A unique identifier for a user''s profile, which corresponds
            to a user''s actual profile or client ID.

            '
          type: string
      type: object
    type: array
type: object
x-akamai:
  file-path: schemas/role-list-item.yaml
