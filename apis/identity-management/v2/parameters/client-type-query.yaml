name: clientType
in: query
description: |
  Filters data by client type, either `USER_CLIENT`, `SERVICE_ACCOUNT`, or default `CLIENT`.
schema:
  type: string
  example: CLIENT
  default: CLIENT
  enum:
  - USER_CLIENT
  - SERVICE_ACCOUNT
  - CLIENT
