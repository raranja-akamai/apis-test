description: |
  Specifies the name for a group.
properties:
  groupName:
    description: |
      A group's name.
    maxLength: 50
    minLength: 1
    type: string
required:
  - groupName
type: object
additionalProperties: false
