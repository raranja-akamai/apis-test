additionalProperties: false
definitions:
  groupAccess:
    properties:
      groupId:
        description: 'The group''s unique identifier.

          '
        type: integer
      groupName:
        description: 'The human-readable name for a group.

          '
        type: string
      isBlocked:
        description: 'If `true`, the API client has access to the group''s parent,
          but cannot access the child group.

          '
        type: boolean
      parentGroupId:
        description: 'The unique identifier for the parent group within the group
          tree. If you''re viewing group info for a root-level group, you may not
          see this member.

          '
        type: string
      roleDescription:
        description: 'The human-readable description for a role.

          '
        type: string
      roleId:
        description: 'A role''s unique identifier.

          '
        type: integer
      roleName:
        description: 'The human-readable name for a role.

          '
        type: string
      subGroups:
        description: 'Children of the parent group, represented as an array of `groupAccess`
          objects. Permissions cascade downward from parent to child unless the child
          group''s `isBlocked` is `true`.

          '
        items:
          description: 'Children of the parent group, represented as an array of `groupAccess`
            objects. Permissions cascade downward from parent to child unless the
            child group''s `isBlocked` is `true`.

            '
          type: object
        type: array
    type: object
  id: groupAccess
  type: object
x-akamai:
  file-path: schemas/group-access.yaml
