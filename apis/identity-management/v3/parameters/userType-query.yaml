description: |
  Indicates the type of users to get, either those who have `lostAccess` or the reverse `gainAccess`.
in: query
name: userType
schema:
  enum:
    - lostAccess
    - gainAccess
  type: string
  example: lostAccess
