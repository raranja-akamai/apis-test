description: 'Optionally filter results by `accountId` or `accountName`. Enter at
  least three characters in the string to filter the results.

  '
in: query
name: search
schema:
  example: 1-2ABCD
  type: string
x-akamai:
  file-path: parameters/search-query.yaml
