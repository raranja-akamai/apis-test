description: Filters results by `accountId` or `accountName`. Enter at least three
  characters in the string to filter the results.
in: query
name: search
schema:
  example: Rae Inc.
  type: string
x-akamai:
  file-path: parameters/search.yaml
