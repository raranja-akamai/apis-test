name: maxItems
in: query
description: |-
  If specified, maximum number of items to return per request, up to the system [limits](doc:limits).
  
x-akamai:
  file-path: parameters/max-items-query.yaml
schema:
  type: integer
  example: 10
