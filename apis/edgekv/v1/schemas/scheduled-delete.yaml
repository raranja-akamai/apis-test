type: object
description: |-
  When a namespace should be deleted.
  
x-akamai:
  file-path: schemas/scheduled-delete.yaml
additionalProperties: false
required:
- scheduledDeleteTime
properties:
  scheduledDeleteTime:
    type: string
    description: |
      Specifies the time when a namespace will be deleted, in ISO 8601 format.
    format: date-time
