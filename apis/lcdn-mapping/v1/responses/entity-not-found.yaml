description: |
  [Not found](ref:404). An HTTP 404 will be returned if the access-map with the specified entityId is not found.
content:
  application/problem+json:
    example:
      $ref: ../examples/entity-not-found.json
    schema:
      $ref: ../schemas/entity-not-found-problem.schema.yaml
