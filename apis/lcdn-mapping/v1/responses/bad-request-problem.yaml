description: |
  [Bad request](ref:400). An HTTP 400 will be returned if the request is badly formed or has constraint violations.
content:
  application/problem+json:
    example:
      $ref: ../examples/constraint-violation.json
    schema:
      $ref: ../schemas/bad-request-problem.schema.yaml
