description: |
  [Conflict](ref:409). An HTTP 409 will be returned if the access-map offered for create/update would lead to a conflicting state.
content:
  application/problem+json:
    example:
      $ref: ../examples/conflicting-property.json
    schema:
      $ref: ../schemas/conflict-problem.schema.yaml
