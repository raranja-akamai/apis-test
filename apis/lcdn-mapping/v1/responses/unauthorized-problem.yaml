description: |
  [Forbidden](ref:403). An HTTP 403 will be returned if the principal is not authorized to access the instance of access-map that the request was made against.
content:
  application/problem+json:
    example:
      $ref: ../examples/unauthorized-access.json
    schema:
      $ref: ../schemas/unauthorized-access-problem.schema.yaml
