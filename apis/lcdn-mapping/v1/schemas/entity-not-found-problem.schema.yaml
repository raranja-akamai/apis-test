description: |-
  Entity not found HTTP problem.  Used when an attempt is made against an entity that does not exist.
  
x-akamai:
  file-path: schemas/entity-not-found-problem.schema.yaml
required:
- type
- instance
- entityType
- entityId
allOf:
- $ref: core-problem.schema.yaml#/definitions/httpProblemType
- properties:
    entityId:
      description: |
        The ID of the referenced entity.
    entityType:
      type: string
      description: |
        The type of entity.
