description: |-
  Invalid parameter HTTP problem.
  
x-akamai:
  file-path: schemas/invalid-parameter-problem.schema.yaml
required:
- type
- instance
- parameter
- value
allOf:
- $ref: core-problem.schema.yaml#/definitions/httpProblemType
- properties:
    value:
      description: |-
        The value that was provided.
        
    parameter:
      type: string
      description: |
        The parameter that is in error.
