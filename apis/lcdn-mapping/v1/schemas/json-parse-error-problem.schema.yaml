description: |-
  JSON parsing error HTTP problem.  Used when the server can't even parse the body as valid JSON.
  
x-akamai:
  file-path: schemas/json-parse-error-problem.schema.yaml
required:
- type
- instance
- lineNumber
- columnNumber
allOf:
- $ref: core-problem.schema.yaml#/definitions/httpProblemType
- properties:
    columnNumber:
      type: integer
    lineNumber:
      type: integer
