type: object
description: |-
  Site Map collection.
  
x-akamai:
  file-path: schemas/site-map.collection.yaml
additionalProperties: false
required:
- maps
- page
properties:
  maps:
    type: array
    description: |
      TBD. A page of SiteMap objects matching the specified filtering and sorting criteria.
    items:
      $ref: site-map.read-summary.yaml
  page:
    $ref: definitions.yaml#/definitions/pageType
