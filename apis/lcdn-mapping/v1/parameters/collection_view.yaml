name: view
in: query
description: |-
  Specifies that either the `detailed` or default `summary` view be returned.
  
x-akamai:
  file-path: parameters/collection_view.yaml
schema:
  type: string
  example: summary
  default: summary
  enum:
  - summary
  - detailed
