name: pageNumber
in: query
description: |-
  Specifies an offset into the results.  Use in conjunction with `pageSize`.  Page number offset is 1-based.
  
x-akamai:
  file-path: parameters/collection_pageNumber.yaml
schema:
  type: integer
  example: 2
  default: 1
  minimum: 1
