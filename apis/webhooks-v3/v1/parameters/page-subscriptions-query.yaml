name: page
in: query
description: |
  Returns subscription information in paginated batches, to minimize bandwidth and speed response time for larger sets of subscription data. Specify the page number you want to return. For example, setting the value to `3` returns only the third batch of subscriptions.

  If you set this value to a page that doesn’t exist (for example, if you set the value to `10` even though you only have six pages of subscriptions) the operation returns the last page of the response. If you leave out this parameter the operation returns all the pages in the response.
x-akamai:
  file-path: parameters/page-subscriptions-query.yaml
required: false
schema:
  type: integer
  example: 3
