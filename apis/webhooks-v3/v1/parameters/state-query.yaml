name: state
in: query
description: |-
  Filters the response data by event state. For example, you can return just the events in the `failure` state, or just the events in the `awaiting_retry` state. Note that you can only specify a single event state in your operation. If you specify more than one state, the operation fails.
  
x-akamai:
  file-path: parameters/state-query.yaml
required: false
schema:
  type: string
  example: failure
  enum:
  - awaiting_retry
  - awaiting_scheduling
  - executing
  - failure
  - success
