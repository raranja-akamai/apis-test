type: object
description: |
  You’re trying to reschedule an event Webhooks v3 can't redeliver. This is typically because the event isn’t in the `failure` state.
x-akamai:
  file-path: schemas/409.yaml
additionalProperties: false
required:
- error
properties:
  error:
    type: string
    description: |
      [Conflict](ref:409).
    minLength: 1
