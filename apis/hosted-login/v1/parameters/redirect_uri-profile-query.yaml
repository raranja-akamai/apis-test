name: redirect_uri
in: query
description: |-
  URL that Hosted Login redirects users to after they close their user profile. You can redirect a user to any URL, including URLs that don't belong to your organization, as long as that URL is in your OpenID Connect client's `redirectURIs` property.
  
x-akamai:
  file-path: parameters/redirect_uri-profile-query.yaml
required: false
schema:
  type: string
  example: https://techdocs.akamai.com/resume
  minLength: 1
