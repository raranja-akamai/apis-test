name: max_age
in: query
description: |-
  Number of seconds before a session automatically expires. After the session expires, users need to reauthenticate if they want to access a protected resource.
  
x-akamai:
  file-path: parameters/max_age-query.yaml
required: false
schema:
  type: integer
  example: 28800
  minimum: 60
