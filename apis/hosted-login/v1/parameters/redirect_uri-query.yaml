name: redirect_uri
in: query
description: |
  URL that Hosted Login redirects a user to after the user logs in. The redirect URL needs to match one of the URLs in the login client’s `redirectURIs` property.
x-akamai:
  file-path: parameters/redirect_uri-query.yaml
required: false
schema:
  type: string
  example: https://techdocs.akamai.com
  minLength: 1
