type: object
description: |-
  One or more items in your path is invalid. Verify each path component and then try again.
  
x-akamai:
  file-path: schemas/404.yaml
additionalProperties: false
required:
- error
properties:
  error:
    type: string
    description: |
      Not found.
    minLength: 1
