type: array
description: |-
  Resource indicators available when using the token policy. Resource indicators are identifiers representing protected resources valid for use with the `resource` parameter.
  
x-akamai:
  file-path: schemas/modify-resource-indicators.yaml
minItems: 0
items:
  type: string
  minLength: 1
