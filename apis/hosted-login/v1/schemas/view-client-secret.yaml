type: object
description: |-
  Returns the client secret for an OpenID Connect (OIDC) client. This operation is the only way to view the secret for a confidential or configuration client.
  
x-akamai:
  file-path: schemas/view-client-secret.yaml
additionalProperties: false
required:
- secret
properties:
  secret:
    type: string
    description: |
      Client secret for the OIDC client.
    minLength: 1
