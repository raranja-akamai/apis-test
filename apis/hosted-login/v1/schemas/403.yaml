type: object
description: |-
  You don't have permission to access the resource. Verify that your access token has the necessary permissions and then try again.
  
x-akamai:
  file-path: schemas/403.yaml
additionalProperties: false
required:
- error
properties:
  error:
    type: string
    description: |
      Forbidden.
    minLength: 1
