type: string
description: |-
  Indicates that the token is no longer valid. To improve security, Hosted Login tells you if it had to revoke the token, or if the token wasn't valid in the first place. You only know that the token isn't valid now.
  
x-akamai:
  file-path: schemas/revoke-token-response.yaml
minLength: 1
