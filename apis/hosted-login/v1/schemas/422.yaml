type: object
description: |-
  This typically means that the JSON in your request didn't pass validation. Verify your JSON syntax and then try again.
  
x-akamai:
  file-path: schemas/422.yaml
additionalProperties: false
required:
- error
properties:
  error:
    type: string
    description: |
      Unprocessable entity.
    minLength: 1
