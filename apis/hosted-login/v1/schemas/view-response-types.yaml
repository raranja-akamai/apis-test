type: array
description: |-
  Returns the response types for a login policy. In an authorization request, the `response_type` parameter specifies the items clients expect to get back from the authorization server after a user logs in. See [Supported response types](https://techdocs.akamai.com/identity-cloud/docs/supported-response-types) for more information.
  
x-akamai:
  file-path: schemas/view-response-types.yaml
minItems: 1
items:
  type: string
  enum:
  - code
  - token
  - id_token
  - id_token token
  - code id_token
  - code token
  - code id_token token
  minLength: 1
