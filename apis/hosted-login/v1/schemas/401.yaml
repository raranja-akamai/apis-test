type: object
description: |-
  Typically occurs because your administrative access token is invalid. Administrative access tokens expire after one hour.
  
x-akamai:
  file-path: schemas/401.yaml
additionalProperties: false
required:
- error
properties:
  error:
    type: string
    description: |
      Authentication required.
    minLength: 1
