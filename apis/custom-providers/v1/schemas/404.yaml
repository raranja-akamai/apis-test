type: object
description: |-
  Typically occurs if your path includes an invalid customer ID or application ID. Verify those values are correct, then try again.
  
x-akamai:
  file-path: schemas/404.yaml
additionalProperties: false
required:
- error
properties:
  error:
    type: string
    description: |
      Resource not found.
    minLength: 1
