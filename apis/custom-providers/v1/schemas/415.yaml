type: object
description: |-
  Typically occurs if the `Content-Type` header is invalid.
  
x-akamai:
  file-path: schemas/415.yaml
additionalProperties: false
required:
- error
properties:
  error:
    type: string
    description: |
      Invalid media type.
    minLength: 1
