type: object
description: |-
  Typically occurs because your token is invalid. Obtain a new access token and then try again. See [Get started](ref:get-started) for more information on configuring custom provider authentication.
  
x-akamai:
  file-path: schemas/403.yaml
additionalProperties: false
required:
- error
properties:
  error:
    type: string
    description: |
      Forbidden.
    minLength: 1
