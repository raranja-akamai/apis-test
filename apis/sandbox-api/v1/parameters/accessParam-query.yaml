name: access
in: query
description: |
  The access level of the request. Either the default `USER` value to perform the request for the sandboxes that you can access, or `ACCOUNT` to perform the request for all sandboxes within the account.
schema:
  type: string
  example: ACCOUNT
  default: USER
  enum:
  - USER
  - ACCOUNT
