type: object
description: |
  Clone an existing sandbox based on its `sandboxId`.
additionalProperties: false
properties:
  name:
    type: string
    description: |
      Descriptive name for the new sandbox.
