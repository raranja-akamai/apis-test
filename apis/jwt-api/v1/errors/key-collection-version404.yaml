description: |-
  [Not found](ref:404). Key collection version doesn't exist.
  
x-akamai:
  file-path: errors/key-collection-version404.yaml
content:
  application/problem+json:
    example:
      $ref: ../examples/example-key-collection-version404error-rs.json
    schema:
      $ref: ../schemas/httpProblemDetails.yaml
