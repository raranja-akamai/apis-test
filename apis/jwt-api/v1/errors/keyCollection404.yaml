description: |-
  [Not found](ref:404). Key collection doesn't exist.
  
x-akamai:
  file-path: errors/keyCollection404.yaml
content:
  application/problem+json:
    example:
      $ref: ../examples/exampleKeyCollection404ErrorRS.json
    schema:
      $ref: ../schemas/httpProblemDetails.yaml
