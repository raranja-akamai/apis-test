description: |-
  [Not found](ref:404). Key collection doesn't exist.
  
x-akamai:
  file-path: errors/key-collection404.yaml
content:
  application/problem+json:
    example:
      $ref: ../examples/example-key-collection404error-rs.json
    schema:
      $ref: ../schemas/httpProblemDetails.yaml
