name: ignorePutPost
in: query
description: |-
  Specifies if the API should ignore ingest data when generating the response, `False` by default.
  
x-akamai:
  file-path: parameters/ignore-put-post-ignore-put-post.yaml
schema:
  type: boolean
  example: true
  default: false
