description: |-
  [Not found](ref:404). Check that the network list ID and other URL parameters are valid.
  
x-akamai:
  file-path: errors/404-client-read-error.yaml
content:
  application/problem+json:
    schema:
      $ref: ../schemas/error.yaml
