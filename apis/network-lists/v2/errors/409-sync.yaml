description: |-
  [Conflict](ref:409). This error occurs if the submitted sync point differs from sync point provided in the most recent retrieve action for this network list.  This happens when different users simultaneously edit the same network list.
  
x-akamai:
  file-path: errors/409-sync.yaml
content:
  application/problem+json:
    schema:
      $ref: ../schemas/error.yaml
