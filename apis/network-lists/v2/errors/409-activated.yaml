description: |-
  [Conflict](ref:409). This occurs if the list is currently activated in staging or production.  Active network lists can't be deleted. Use the status operations to determine if this list is active.
  
x-akamai:
  file-path: errors/409-activated.yaml
content:
  application/problem+json:
    schema:
      $ref: ../schemas/error.yaml
