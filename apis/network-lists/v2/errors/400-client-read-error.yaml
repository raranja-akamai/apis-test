description: |-
  [Incorrect client input](ref:400). See the field errors provided in the response for more information.  This error may also be returned in place of a 404 if a URL parameter was provided in an incorrect format.
  
x-akamai:
  file-path: errors/400-client-read-error.yaml
content:
  application/problem+json:
    schema:
      $ref: ../schemas/error.yaml
