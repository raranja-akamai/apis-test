description: |-
  Incorrect URL. Check that the network list ID and other URL parameters are valid.
  
x-akamai:
  file-path: errors/client-read-errors-404.yaml
content:
  application/problem+json:
    schema:
      $ref: ../schemas/error.yaml
